services:
  - type: web
    name: sample-bank
    env: java
    plan: free
    buildCommand: ./mvnw clean install
    startCommand: java -jar target/*.jar
    envVars:
      - key: PORT
        value: 10000
