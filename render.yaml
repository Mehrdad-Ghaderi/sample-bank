services:
  - type: web
    name: sample-bank
    env: java
    plan: free
    buildCommand: ./mvnw clean install
    startCommand: java -jar target/sample-bank-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17

build:
  environment:
    JAVA_VERSION: 17
