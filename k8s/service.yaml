apiVersion: v1
kind: Service
metadata:
  name: sample-bank-service
  namespace: sample-bank
spec:
  selector:
    app: sample-bank
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: LoadBalancer
